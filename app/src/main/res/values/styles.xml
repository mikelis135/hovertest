<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">#4a4a4a</item>
        <item name="buttonStyle">@style/ButtonStyle</item>
        <item name="alertDialogStyle">@style/dialogStyle</item>
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@drawable/button_round</item>
        <item name="android:typeface">normal</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="dialogStyle" parent="Theme.MaterialComponents.Light.Dialog.Alert">
        <item name="buttonBarNegativeButtonStyle">@style/buttonBarAlertStyle</item>
        <item name="buttonBarPositiveButtonStyle">@style/buttonBarAlertStyle</item>
    </style>

    <style name="buttonBarAlertStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">

        <item name="android:textColor">@color/colorAccent</item>

    </style>

</resources>
